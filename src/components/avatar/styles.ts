import { StyleSheet } from "react-native";

export const styles= StyleSheet.create({
    container:{
        width: 60,
        height:60,
        borderRadius: 45, 
        alignItems: 'center',
        justifyContent: 'center',
        marginRight: 22
    },
    
    avatar:{
        width: 57,
        height: 57,
        borderRadius: 45
    }
});