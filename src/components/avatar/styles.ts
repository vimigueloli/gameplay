import { StyleSheet } from "react-native";

export const styles= StyleSheet.create({
    container:{
        width: 53,
        height:53,
        borderRadius: 10, 
        alignItems: 'center',
        justifyContent: 'center',
        marginRight: 22
    },
    
    avatar:{
        width: 50.5,
        height: 50.5,
        borderRadius: 10
    }
});